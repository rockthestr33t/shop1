1.
Onion Market's url's for inspiration :
https://saralaajivika.000webhostapp.com/
https://gist.github.com/smarek/b538465a711edc7e6d7652c82e76d374
https://github.com/nishu-aggarwal/MultiVendorWebsite
https://github.com/1234max/MultiVendorBitcoinMart/wiki
https://shuup.readthedocs.io/en/latest/index.html
https://github.com/puuble/laravelMarket
https://github.com/kirilkirkov/Ecommerce-CodeIgniter-Bootstrap

# 2.
# ADD to script disable apache2 on startup :
sudo update-rc.d apache2 disable

# 3.
# ADD to script nginx-extras :
sudo apt install nginx-extras

# 4.
# ADD to script 'html & htm' indexe's :
sudo nano /etc/nginx/sites-available/default & annularis-nginx.conf
add   index.html index.htm index.nginx-debian.html

# 5.
# Admin info search all 'php7.0 option's' to install from repository :
sudo apt-cache search php7.0

# 6.
# Admin info list all unix socket's :
sudo netstat --unix -l

# 7.
# Admin info list all tcp socket's :
sudo netstat -t -l

# 8.
# ADD to script
# changing permission nginx rockthestreet81 and chmod :
# Change directory structure ownership for the new directory to www-data:www-data for the webserver. 
# from oter tutorial
# sudo chown -R www-data:www-data /var/www/shop
# this i used
sudo chown www-data:www-data -Rv /var/www/shop


# 9.
# Give www-data user/group read/write/execute privileges for all directories, and read/write privileges for all files, within the project directory. 
sudo find /var/www/shop -type d -exec chmod 770 {} \;
sudo find /var/www/shop -type f -exec chmod 660 {} \;
# sudo chown user:user -R .
# this i used
sudo chown www-data:www-data -Rv /var/www/shop

# 9.
# Check if installed and we have to install next time?  download ping and fpm status site :
sudo git clone https://github.com/wwwpro/wordpress-nginx/blob/master/php_fpm_status_vhost.conf
sudo git clone https://github.com/wwwpro/wordpress-nginx/blob/master/php_fpm_status_allowed_hosts.conf

# to request this page, you have to set it up in fpm and configure nginx (or another front web server) to access it.

# For exemple in php-fpm.conf:
# <value name="status">/status</value> // requesting /status will
# return the status page
# <value name="ping">/ping</value> // requesting /ping will return the
# text set next
# <value name="pong">pong</value> // requestint the previous parameter
# will return pong

# And in nginx.conf:
# ...
 http {
 server {
 listen 80;
# ...
 location ~ ^/(status|ping)$ {
 include fastcgi_params;
 fastcgi_pass backend;
 fastcgi_param SCRIPT_FILENAME $fastcgi_script_name;
 allow 127.0.0.1;
 allow stats_collector.localdomain;
 allow watchdog.localdomain;
 deny all;
 }
 }
 }

# The request the web server:
# curl http://localhost:81/status.php
accepted conn: 2
pool: pool_1
process manager: static
idle processes: 3
active processes: 0
total processes: 3

# curl http://localhost:81/status.php?html
<table>
<tr><th>accepted conn</th><td>3</td></tr>
<tr><th>pool</th><td>pool_1</td></tr>
<tr><th>process manager</th><td>static</td></tr>
<tr><th>idle processes</th><td>3</td></tr>
<tr><th>active processes</th><td>0</td></tr>
<tr><th>total processes</th><td>3</td></tr>

# curl http://localhost:81/status.php?json
{"accepted conn":4,"pool":"pool_1","process manager":"static","idle
processes":3,"active processes":0,"total processes":3}

# curl http://localhost:81/ping.php
# pong



# try to set php-fpm to /run.... and the other config to /var/run

# try to size up the cache ram

# give user annularis a password login as this user and try a few things give him root rights etc...

# change index.php to /index.php in /etc/nginx/sites-enabled/default & annularis conf

# add index.htm, index.html, .... and so on

# it seems running 2 php-fpm's the normal and the annularis . Delete the normal


 add robot.txt from bitwasp/W1s

# add user and group with password install this tool 
sudo apt-get install gnome-system-tools
# this url for add user and group: https://www.digitalocean.com/community/tutorials/how-to-create-a-sudo-user-on-ubuntu-quickstart
# this url for changing permission: https://askubuntu.com/questions/66718/how-to-manage-users-and-groups-using-gui
# after that change to new user

# 
sudo nano /etc/php/7.0/fpm/php.ini  # change phpcgi_fixpathinfo = 0 and memory_limit to 256M












